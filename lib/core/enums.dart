enum DeviceAttributes {
  brightness,
  speed,
  cooling,
  channel,
  temperature,
  mode,
}

enum ThemePrefrences { light, dark }

enum DeviceType {
  bulb,
  fan,
  ac,
  tv,
  oven,
  washingMachine,
}
